export const metadata = {
  title: "About",
  description:
    "Learn how Feed Me Budapest selects restaurants, reviews recommendations monthly, uses AI responsibly and keeps its restaurant suggestions independent.",
  alternates: {
    canonical: "/about",
  },
};

export default function AboutLayout({ children }) {
  return children;
}
